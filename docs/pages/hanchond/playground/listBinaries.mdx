# List Binaries

The command `list-binaries` can be used To get a list of all the binaries that are available to use inside the `playgroud`

## Command

```sh
hanchond playground list-binaries
Binaries:
        - evmosdv18.1.0
        - hermes
```
