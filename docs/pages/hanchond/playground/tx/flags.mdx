# Flags

When sending a command under the section `tx` there is a global flag that can be set, to determine the node that will execute the transaction

```sh
hanchond playground tx --node 1
```

:::info
The node ID is the one that you got after running `evmos-start`
::::
