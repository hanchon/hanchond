# Flags

When sending a command under the section `query` there is a global flag that can be set, to determine the node that will execute the transaction

```sh
hanchond playground query --node 1
```

:::info
The node ID is the one that you got after running `evmos-start`
::::
