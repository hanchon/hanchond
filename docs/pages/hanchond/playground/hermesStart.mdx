# Hermes Start

The `start-hermes` command can be used to run the relayer.

## Command

Hermes start will only be executed correctly if the relayer is not already running

```sh
hanchond playground start-hermes
Hermes running with PID: 39147
```

:::info
This command assumes that the relayer was already built
:::
