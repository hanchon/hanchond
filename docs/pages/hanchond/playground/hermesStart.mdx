# Hermes Start

The `hermes-start` command can be used to run the relayer.

## Command

Hermes start will only be executed correctly if the relayer was not already running

```sh
hanchond playground hermes-start
Hermes running with PID: 39147
```

:::info
This command assumes that the relayer was already built
:::
